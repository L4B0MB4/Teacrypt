{
  "name": "teacrypt",
  "version": "1.0.0",
  "description": "## How to use in dev:",
  "private": "true",
  "scripts": {
    "run-server": "lerna run --parallel production",
    "dev": "lerna run --parallel start",
    "build-server": "lerna run --parallel build-production",
    "build-all": "lerna run --parallel build",
    "test": "lerna run --parallel test",
    "install": "lerna run --parallel install && lerna run --parallel prepare"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/L4B0MB4/Teacrypt.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/L4B0MB4/Teacrypt/issues"
  },
  "homepage": "https://github.com/L4B0MB4/Teacrypt#readme",
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
